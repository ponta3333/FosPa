<div class="row">
	<div class="text-center">
		<h2><span class="underbar">FosPaへようこそ！</span></h2>
		<p>このサイトは犬猫を家族として迎えたい方、もらい手を探している方を結ぶサイトです。</p>
	</div>
	<div class="text-center">
		<h3>どちらをお探しですか？</h3>
		<% @genres.each do |genre| %>
    	<%= link_to genre_path(genre) do %>
				<div class="choice hover">
					<i class="fas fa-paw"><%= genre.name %></i>
     		</div>
     	<% end %>
    <% end %>
	</div>
	<div class="shintyaku">
		<h3><span class="underbar">新着情報</span></h3>
		<div class="bxslider">
			<% @animals.each do |animal| %>
				<% if animal.sex == "オス" %>
					<div class="slidecontent_an hover">
						<div class="animalmale_top">
							<%= link_to animal_path(animal) do %>
								<%= attachment_image_tag animal, :image, format: 'jpeg', size:"200x200", class: "animal-image" %>
									<p><%= animal.name %></p>
							<% end %>
						</div>
					</div>
				<% else %>
					<div class="slidecontent_an hover">
						<div class="animalfemale_top">
							<%= link_to animal_path(animal) do %>
								<%= attachment_image_tag animal, :image, format: 'jpeg', size:"200x200", class: "animal-image" %>
									<p class="animalfont"><%= animal.name %></p>
							<% end %>
						</div>
					</div>
				<% end %>
			<% end %>
		</div>
	</div>
	<div class="shintyaku">
		<h3><span class="underbar">新着イベント</span></h3>
		<div class="bxslider">
			<% @events.each do |event| %>
				<div class="slidecontent_ev hover">
					<div class="posted_top">
						<%= link_to event_path(event) do %>
							<%= attachment_image_tag event, :image, format: 'jpeg', size:"190x190" %>
							<p><span class="index-name"><%= event.name %></span></p>
							<p><%= event.day.strftime("%Y/%m/%d") %></p>
							<p><%= event.prefecture %></p>
						<% end %>
					</div>
				</div>
			<% end %>
		</div>
	</div>
</div>