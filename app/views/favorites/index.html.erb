<div class="row">
	<h2><span class="underbar">気になるリスト</span></h2>
	<div class="col-xs-12 text-center index-space">
		<div class="row">
			<% if @favorites.exists? %>
				<% @favorites.each do |favorite| %>
					<% animal = Animal.find_by(id: favorite.animal_id) %>
					<% if animal.sex == "オス" %>
						<div class="col-xs-3 animalmale">
							<div>
								<%= link_to animal_path(animal) do %>
									<%= attachment_image_tag animal, :image, format: 'jpeg', size:"230x230", class: "animal-image" %>
										<p><%= animal.name %></p>
										<p><%= animal.bleed.name %>/<%= animal.sex %>/<%= animal.age %></p>
								<% end %>
							</div>
						</div>
					<% else %>
						<div class="col-xs-3 animalfemale">
							<div>
								<%= link_to animal_path(animal) do %>
									<%= attachment_image_tag animal, :image, format: 'jpeg', size:"230x230", class: "animal-image" %>
										<p class="animalfont"><%= animal.name %></p>
										<p class="animalfont"><%= animal.bleed.name %>/<%= animal.sex %>/<%= animal.age %></p>
								<% end %>
							</div>
						</div>
					<% end %>
				<% end %>
			<% else %>
				<h3>[気になるリスト追加]ボタンを押すとココに表示されます</h3>
			<% end %>
		</div>
	</div>
</div>
