<div class="row">
	<h2 class="title-left"><span class="underbar">パートナー検索（<%= @genre.name %>）</span></h2>
	<div class="col-xs-12 text-center index-space">
		<div class="row">
			<% if @animals.exists? %>
			<% @animals.each do |animal| %>
				<% if animal.sex == "オス" %>
					<div class="col-xs-3 animalmale hover">
						<div>
							<%= link_to animal_path(animal) do %>
								<%= attachment_image_tag animal, :image, format: 'jpeg', size:"230x230", class: "animal-image" %>
									<p><%= animal.name %></p>
									<p><%= animal.bleed.name %>/<%= animal.sex %>/<%= animal.age %></p>
							<% end %>
						</div>
					</div>
				<% else %>
					<div class="col-xs-3 animalfemale hover">
						<div>
							<%= link_to animal_path(animal) do %>
								<%= attachment_image_tag animal, :image, format: 'jpeg', size:"230x230", class: "animal-image" %>
									<p class="animalfont"><%= animal.name %></p>
									<p class="animalfont"><%= animal.bleed.name %>/<%= animal.sex %>/<%= animal.age %></p>
							<% end %>
						</div>
					</div>
				<% end %>
			<% end %>
			<% else %>
			<h3>現在、募集がありません</h3>
			<% end %>
		</div>
	</div>
</div>
<div class="row">
  <div class="col-xs-8 col-md-offset-2">
    <div class="text-center">
      <%= paginate @animals %>
    </div>
  </div>
</div>